use_default_settings: true

server:
  bind_address: 0.0.0.0
  port: 8080
  secret_key: "CHANGE_ME_LONG_RANDOM"

ui:
  default_locale: "en"

# Enable your plugin (module path matches where we copied it)
plugins:
  searx.plugins.ai_summarize_select_fetch.SXNGPlugin:
    active: true
